# 2026-04-16 验收证据索引

## 代码与接口

- `web` 构建：`npm run build` 通过
- `web` lint：`npm run lint` 通过
- `GET /api/health`：200
- `GET /api/models`：200，当前返回 26 个模型
- 26 个模型最小文本请求：见 `audit/model-acceptance-table-2026-04-16.md`
- 11 个视觉模型逐个图片回归：见 `audit/vision-model-acceptance-table-2026-04-16.md`

## 本轮新增浏览器截图

### 1366x768

- `light-theme-notice-1366.png`
  - 用途：验证浅色主题下黄色 `notice-pill` 的可读性
- `soft-theme-notice-1366.png`
  - 用途：验证柔和主题下黄色 `notice-pill` 的可读性
- `models-refresh-soft-1366.png`
  - 用途：验证模型页显示 26 个模型、自动检测与手动重测入口存在
- `knowledge-soft-1366.png`
  - 用途：验证知识页导览压缩后，首屏直接看到主知识卡片首排
- `drawer-top-soft-1366.png`
  - 用途：验证抽屉顶部内容与覆盖式展开状态
- `drawer-bottom-soft-1366.png`
  - 用途：验证抽屉底部帮助、免责声明、产品介绍完整可达

### 1440x900

- `latest-chat-1440x900.png`
  - 用途：验证聊天默认态在较高桌面分辨率下的主区聚焦与首屏可达性
- `latest-models-1440x900.png`
  - 用途：验证模型页默认态在较高桌面分辨率下的双列布局完整性
- `latest-knowledge-1440x900.png`
  - 用途：验证知识页默认态在较高桌面分辨率下首屏直接看到主知识卡片

### 1920x1080

- `latest-chat-1920x1080.png`
  - 用途：验证大屏聊天默认态下顶栏压缩后主聊天区仍然是第一焦点
- `latest-knowledge-1920x1080.png`
  - 用途：验证大屏知识页默认态下主知识卡片区仍然压过导览区

### 768x1024

- `latest-chat-768x1024.png`
  - 用途：验证平板竖屏聊天页的主输入链路与消息区可达性
- `latest-knowledge-768x1024.png`
  - 用途：验证平板竖屏知识页首屏仍能直接看到主知识卡片

### 390x844

- `latest-chat-390x844.png`
  - 用途：验证手机竖屏聊天页默认态
- `latest-drawer-390x844.png`
  - 用途：验证手机竖屏抽屉覆盖式展开与内容可达性
- `latest-knowledge-390x844.png`
  - 用途：验证手机竖屏知识页默认态

## 本轮实际浏览器复测结论

- 抽屉
  - 覆盖式抽屉未挤压主区。
  - 抽屉主体是唯一纵向滚动拥有者。
  - 实测滚到最底部后，`使用帮助`、`免责声明`、`产品介绍 / 使用方式` 都可完整看到。
- 知识页
  - 首屏按钮显示 `全部内容 · 21`，恢复为完整条目数量，不再只有 7 条。
  - 导览、阅读提示和病例图说明均已压缩成按需展开，不再长期占住首屏。
  - 在 `1366x768` 下首屏直接能看到主知识卡片首排顶部。
- 切换模型保留草稿
  - 复测步骤：在聊天页重新输入文字并重新上传 `cdc-phil-4506-eczema.jpg`，随后切换到 `GPT-5.4 mini`。
  - 结果：新会话中图片预览仍在，文本草稿仍在，页面同时显示“已切换到 GPT-5.4 mini，并保留了当前输入文字和图片草稿”。
- 手动连通性测试
  - 模型页重测前：`最近检测 19:31:04`
  - 点击按钮后：按钮文本变为 `检测中`
  - 重测结束后：更新为 `最近检测 19:31:52`
  - 结论：`每 5 秒自动检测 + 手动重测` 当前可用。
- 主题下黄色提示字
  - 浅色主题与柔和主题均重新截图检查。
  - `notice-pill` 在两种浅色系主题下都已切到更深的黄棕色文字，不再出现“黄字发白看不清”。
- 浏览器 console
  - 当前 Playwright 复核会话 `error` 级别消息为 0 条。
- 视觉模型逐个喂图回归
  - 已对 11 个 `supportsVision = true` 的模型逐个请求 `POST /api/vision`。
  - 结果：11 / 11 请求成功，11 / 11 返回非空正文，4 个模型真实上游返回，7 个模型触发 mock fallback。
  - 结论：图片上传、`/api/vision` 路由、前端显示链路已闭环，剩余风险收口为部分上游当前不可用。
- 响应式截图矩阵
  - 已补记 `1440x900`、`1920x1080`、`768x1024`、`390x844` 的最新聊天 / 模型 / 抽屉 / 知识页截图记录。

## 仍需持续关注

- 当前 26 个模型里有 9 个在本轮最小文本请求时触发了 mock fallback，问题在上游连通性，不在前端入口链路。
- 11 个视觉模型逐个喂图回归已完成；其中 7 个模型当前仍走 mock fallback，剩余风险同样在上游可用性，不在前端图片链路或页面呈现。

## 2026-04-17 补充验收：运行实例识别与错实例排除

### 启动链路与实例识别

- 前端启动脚本当前固定为：
  - `vite --host 127.0.0.1 --port 5173 --strictPort`
- 后端实例当前固定为：
  - `http://127.0.0.1:8787`
- 复测 `web` 目录再次执行 `npm run dev`：
  - 结果直接报错 `Port 5173 is already in use`
  - 结论：前端不会再静默漂移到其他端口
- 浏览器实际核对结果：
  - `#root` 与 `document.documentElement` 均挂有 `data-build-id="skin-runtime-2026-04-17-r1"`
  - `window.__SKINCAREAI_RUNTIME__` 可读到：
    - `modelCount = 26`
    - `knowledgeCount = 21`
    - `routeMarker = chat-models-26 / knowledge-all-21`
  - Console 可见 `[SkinCareAI runtime]`

### 本轮新增浏览器截图

- `runtime-chat-1366.png`
  - 用途：验证当前打开的就是带 dev 指纹的新实例，且顶栏直接显示 `聊天工作区 / 知识页`
- `chat-uploaded-draft-1366.png`
  - 用途：验证新会话输入文字并上传图片后，聊天区仍保持主交互地位，没有被图片预览过度挤压
- `chat-switch-model-preserve-draft-1366.png`
  - 用途：验证从文本模型切到视觉模型后，当前文字草稿和图片草稿一起迁移到新会话
- `chat-vision-response-1366.png`
  - 用途：验证新会话图文请求返回简体中文，且回答结构先给“最可能的疾病或问题”，再给判断依据与中医方向
- `light-theme-notice-current.png`
  - 用途：验证浅色主题下提示性黄字在当前实例中可读

### 本轮补充浏览器复测结论

- `/knowledge`
  - 页面首屏按钮明确显示 `全部内容 · 21`
  - 当前路由指纹为 `knowledge-all-21`
- 模型工作台
  - 当前显示 `26 个模型`
  - 手动入口当前文案已明确为 `连通性测试 / 连通性测试中`
  - 自动检测复测：
    - 先看到 `最近检测 04:59:38`
    - 6 秒后自动刷新为 `最近检测 05:00:06`
  - 手动重测复测：
    - 点击后进入 `连通性测试中`
    - 随后更新为 `最近检测 05:01:16`
- 模型切换保草稿
  - 复测路径：
    - 新建会话
    - 输入“请根据这张皮肤图片判断最可能是什么病，并给出中医辨证和中药方向。”
    - 上传 `cdc-phil-4506-eczema.jpg`
    - 点击 `切换到 Qwen 3 VL Flash`
  - 结果：
    - 新会话标题仍为 `新会话`
    - 当前模型切换为 `Qwen 3 VL Flash`
    - 图片预览仍在
    - 文本草稿完整保留
- 新会话回答方向
  - 直接在新会话发送图文请求后，回答正文首段明确出现：
    - `最可能的疾病或问题：`
    - `判断依据：`
    - `还需要确认的信息：`
    - `西医护理建议：`
  - 结论：新提示词链路已生效，不能再用旧会话里的旧英文回复判断当前版本

### 本轮工程校验

- `cd web && npm run build`：通过
- `cd web && npm run lint`：通过
