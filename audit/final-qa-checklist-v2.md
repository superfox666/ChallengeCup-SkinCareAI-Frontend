# SkinCareAI 最终 QA 清单 v2

更新日期：2026-04-16

状态定义：
- `通过`：本轮已实际运行或截图确认。
- `部分通过`：代码与入口在，但证据不够完整，仍建议补一次人工复测。
- `未通过`：当前仍有真实阻塞。

## 1. 工程健康

| 项目 | 状态 | 备注 |
| --- | --- | --- |
| `web` 构建 | 通过 | `npm run build` 通过 |
| `web` lint | 通过 | `npm run lint` 通过 |
| 浏览器控制台 error | 通过 | Playwright 当前页 `0 error` |
| `/api/health` | 通过 | 本轮重启后端后正常 |
| `/api/models` | 通过 | 返回 16 个模型 |

## 2. 首屏与布局

| 项目 | 状态 | 备注 |
| --- | --- | --- |
| `/chat` 首屏不依赖整页纵向滚动 | 通过 | `1366x768` 下页面无整体纵向滚动 |
| `/knowledge` 首屏不依赖整页纵向滚动 | 通过 | `1366x768` 下页面无整体纵向滚动 |
| Logo 左上角显著可见 | 通过 | 顶栏常驻 |
| 顶栏压缩后仍保留必要状态 | 通过 | 当前模型 / 消息数 / disclaimer 短提示保留 |
| 抽屉展开覆盖页面、不挤压主区 | 通过 | `chat-1366-drawer.png` |
| 标签条改为轻量化 icon-first | 通过 | `chat-1366-default.png` |
| 主聊天区明显大于低频说明区 | 通过 | `/chat` 首屏已成立 |

## 3. 聊天主链路

| 项目 | 状态 | 备注 |
| --- | --- | --- |
| 新建会话 | 通过 | 抽屉入口保留 |
| 发送文本 | 通过 | 本轮已发送并返回 |
| 流式返回 | 通过 | `/api/chat/stream` 浏览器网络 200 |
| 复制回答 | 通过 | 回答卡按钮存在并可点 |
| 复制会话 | 通过 | 成功提示恢复 |
| 导出 Markdown | 通过 | Playwright 成功下载 `.md` |
| 原生分享 | 部分通过 | 按钮与逻辑保留，浏览器环境无法完整替代真机 |
| 文本模型图片 handoff | 通过 | 代码链路保留，提示文案恢复 |
| 图片上传按钮 | 通过 | 输入区入口正常 |
| 图片拖拽 / 粘贴 | 部分通过 | 本轮未重新截屏，建议补一轮人工复测 |

## 4. 模型工作台

| 项目 | 状态 | 备注 |
| --- | --- | --- |
| 模型分类按钮加大 | 通过 | 已改为横排大按钮 |
| 模型数量未减少 | 通过 | `/api/models` 16 个 |
| 模型状态语义纠正 | 通过 | “可用 / 未测速” 已生效 |
| 模型页内部滚动到底 | 通过 | 可滚到 `Qwen Plus Latest` |
| 切换模型自动新建会话 | 通过 | 已实测切到 `GPT-5.4 mini` |
| 模型逐个可选 | 通过 | 逐测表见 `model-acceptance-table-2026-04-16.md` |
| 模型逐个可发请求 | 通过 | 16/16 返回 200；其中 3 个走 mock fallback |

## 5. Knowledge 与会诊

| 项目 | 状态 | 备注 |
| --- | --- | --- |
| `knowledge` 首页结构 | 通过 | 精简导览、分类 chip、案例演示集齐全 |
| `knowledge` 分类切换 | 通过 | 已切换“西医常见问题” |
| “待补”占位文案清理 | 通过 | 不再直接对外暴露 |
| 多专家会诊 | 通过 | 顶栏按钮可打开 |
| 裁决者总结 | 通过 | 已生成总结区 |
| 会诊不挤压主聊天区 | 通过 | 右侧覆盖式 Sheet |

## 6. 响应式截图

| 视口 | 状态 | 证据 |
| --- | --- | --- |
| `1366x768` | 通过 | `chat-1366-default.png`、`knowledge-1366-default.png` |
| `1440x900` | 通过 | `chat-1440-default.png` |
| `1920x1080` | 通过 | `chat-1920-default.png` |
| `768x1024` | 通过 | `chat-768x1024-default.png` |
| `390x844` | 通过 | `chat-390x844-default.png` |

## 7. 仍建议继续补的边界项

- 真机环境下的系统分享面板。
- 图片拖拽、图片粘贴的再次可视化截图。
- 会话重命名、会话删除的再次可视化截图。
- 视觉模型的逐个图片输入验收；本轮逐测是“文本最小连通性”。
